[DEFAULT]
VENV = /opt/argo-automation-cpas/

[general]
loggers = stdout, file, syslog
log_level = INFO
log_file = /opt/argo-automation-cpas/var/log/argo-cpas.log
syslog_address = /dev/log
syslog_facility = user

[ams]
project = ARGO-MON-AUTOMATION
host = api.devel.msg.argo.grnet.gr
subscription = events-sub-2
token = AUTOMATION_TOKEN

[webapi]
host = api.devel.mon.argo.grnet.gr

[iam]
host = https://login-devel.einfra.grnet.gr/auth/realms/einfra/protocol/openid-connect/token
oidc_client_id = client-id
oidc_client_secret = client-secret

[ansible]
user_connector = user
group_connector = group
ssh_private_key = /home/user/.ssh/sshkey

# vim: ft=conf
